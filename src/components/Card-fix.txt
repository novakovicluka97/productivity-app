Lines 56-63 need to be replaced with conditional hook:

// Music playback hook - ONLY for active card to avoid conflicts
const audioHook = card.isActive ? useCardAudio({
  selectedTrack: card.selectedTrack,
  volume: card.volume || 50,
  isMusicPlaying: card.isMusicPlaying || false,
  isActive: card.isActive
}) : { isPlaying: false, togglePlayPause: () => {}, stop: () => {} }

const { isPlaying: isMusicPlaying, togglePlayPause } = audioHook
